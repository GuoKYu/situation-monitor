# 锁定 Node 版本，避免兼容问题
use-node-version=20.18.0
# 锁定 pnpm 版本，适配 Node 20.x
package-manager=pnpm@9.12.3
# 切换稳定的 npm 镜像源，解决元数据拉取失败
registry=https://registry.npmmirror.com
# 禁用严格的 lockfile 检查，避免版本冲突
strict-peer-dependencies=false
